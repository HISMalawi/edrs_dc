<style type="text/css">
  
     
</style>
<div id="default" style="width: 90%;margin:auto; height: 400px; text-align: center; vertical-align: top;">
    <div id="buttons" style="display: inline ">
         <% if UserModel.current_user.activities_by_level("DC").include?("Request to open a closed case") %>
             <div class="touch_btn" " onmousedown="window.location = '/dc/reprint_requests?next_url=/dc/manage_requests?next_url=/'" >
                 <div class="icon">
                      <i class="fa fa-folder-open"></i>
                  </div>
                  <div class="button_label">Lost/Damaged cases</div>
                  <div class="button_description">  </div>                                 
             </div>

         <%end%>
        <% if UserModel.current_user.activities_by_level("DC").include?("Request to open a closed case") %>
              <div class="touch_btn" onmousedown="window.location = '/dc/amendment_requests?next_url=/dc/manage_requests?next_url=/'" >
                  <div class="icon">
                        <i class="fa fa-folder-open"></i>
                   </div>
                   <div class="button_label">Amendments</div>
                    <div class="button_description">  </div>                               
              </div>
          <%end%>
           <% if UserModel.current_user.activities_by_level("DC").include?("Request to open a closed case") %>
                  <div class="touch_btn" onmousedown="window.location = '/printed_amendmets'" >
                      <div class="icon">
                                           <i class="fa fa-folder-open"></i>
                            </div>
                             <div class="button_label">Printed/Dispatched Certificates </div>
                            <div class="button_description">  </div>                               
                  </div>
           <%end%>         
                                      
    </div>
</div>
<%= render :partial => "landing_resize" %>

