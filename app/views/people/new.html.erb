<script type="text/javascript">
	var facility_type = "<%= SETTINGS['site_type'] %>"
</script>
<style type="text/css">
	.capitalize{
		 text-transform: capitalize;
	}
</style>
<% if params[:registration_type].include? "Natural" %>
	<%= render 'form' %>
<%elsif params[:registration_type].include? "Unclaimed"%>
	<%= render 'special' %>
<%elsif params[:registration_type].include? "Deaths Abroad"%>
	<%= render 'abroad' %>
<%else%>
	<%= render 'form' %>
<%end%>